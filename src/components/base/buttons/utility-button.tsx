/**
 * Adaptador de nombre: los componentes custom del kit importan
 * `UtilityButton` desde esta ruta; el componente real del sistema
 * es `ButtonUtility` (mismas props).
 */
export { ButtonUtility as UtilityButton } from "@/components/base/buttons/button-utility";
